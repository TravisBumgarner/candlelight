Holy convoluted Batman.

No new lines in Google Sheets Levels doc.
Strings need to be double quoted.

1. Setup virtual env `python -m venv venv` then `source ./venv/bin/activate` then `pip install -r requirements.txt`
1. Load up levels previewer (with that sick sick hot reloading)`python watch_and_generate.py`
1. Turn on a screen recording (some of these are tricky to recreate)
1. Load level editor and start levelin'
1. Populate new levels and worlds in ingestion
1. Execute script. `python main.py`
1. Copy levels over to Godot and / or React Native.
1. Verify git diff.
1. Send it.
